<div id="banner" class="podcasts_banner">
  <h1>Discover your new favorite podcast</h1>
</div>

<div id="podcasts" class="clearfix">
  <div class="wrapper">
    <% @podcasts.each do |podcast| %>
    <%= link_to podcast do %>
    <ul class="clearfix">
        <li>
        <%= image_tag podcast.thumbnail.url(:medium) %>
          <div class="podcast_overview">
            <h2><%= podcast.title %></h2>
            <p class="episode_count"><%= podcast.episodes.count %> Episode</p>
          </div>
        </li>
    <% end %>
    <% end %>
    </ul>
    <%= will_paginate @podcasts %>
  </div>
</div>